{
  "name": "webengP",
  "version": "1.0.0",
  "scripts": {
    "test": "npx htmlhint \"**/*.html\" && npx stylelint \"**/*.css\" && npx jsonlint \"**/*.json\""
  },
  "devDependencies": {
    "htmlhint": "^1.9.2",
    "jsonlint": "^1.6.3",
    "stylelint": "^17.6.0",
    "stylelint-config-standard": "^40.0.0"
  }
}
